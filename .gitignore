# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# OS
.DS_Store

# Model weights — fetched with `make models` / scripts/fetch_deepfake_detector.py.
# GitHub rejects files over 100 MB, and these are 130–330 MB each.
models/**/*.onnx
models/**/*.onnx.data
models/**/*.zip
models/insightface/

# Downloaded datasets (scikit-learn LFW cache, ~1.8 GB)
data/sklearn/
